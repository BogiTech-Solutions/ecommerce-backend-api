server:
  port: 8080

spring:
  application:
    name: ecommerce-api
  datasource:
    url: jdbc:postgresql://ep-noisy-salad-axa2vu4d.c-4.us-east-2.aws.neon.tech:5432/neondb?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_q0YWwGkfxMl9
    driver-class-name: org.postgresql.Driver

  # datasource:
  #   url: jdbc:postgresql://neondb_owner:npg_q0YWwGkfxMl9@ep-noisy-salad-axa2vu4d.c-4.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require
  #   username: neondb_owner
  #   password: npg_q0YWwGkfxMl9
  #   driver-class-name: org.postgresql.Driver


  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: ERROR
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
application:
  security:
    jwt:
      secret: ${T_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
      expiration: ${T_EXPIRATION_MS:86400000}
payment:
  stripe:
    secret-key: ${STRIPE_SECRET_KEY:sk_test_placeholder}
    webhook-secret: ${STRIPE_WEBHOOK_SECRET:whsec_test_placeholder}
  chapa:
    secret-key: ${CHAPA_SECRET_KEY:CHASECK_TEST_placeholder}
    webhook-secret: ${CHAPA_WEBHOOK_SECRET:chapa_webhook_secret_placeholder}
    base-url: https://api.chapa.co/v1
